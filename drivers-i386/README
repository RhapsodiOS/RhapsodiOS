Current list of drivers to be implemented and the status of re-implementation
=====================================================

bus
 * drvEISABus - crashing, needs debugging
 * drvPICBus - complete
 * drvPCMCIABus - needs compiled and then tested
 * Intel824X0PCI - complete
 * Intel82365PCMCIA - needs compiled and then tested

ide
 * drvEIDE - complete
 * drvPCFloppy - needs compiled and then tested

input
 * drvBusMouse - needs compiled and then tested
 * drvISASerialPort - needs compiled and then tested
 * drvPCParallel - needs compiled and then tested
 * drvPS2Keyboard - needs compiled and then tested
 * drvPS2Mouse - needs compiled and then tested
 * drvSerialPointingDevice - needs compiled and then tested

network
 * drvAMDPCNet32II - complete
 * drvDEC21X4X - needs compiled and then tested
 * drvDEC21142 - needs compiled and then tested
 * drvDECchip21040 - needs compiled and then tested
 * drvDECchip21140 - needs compiled and then tested
 * drvEtherExpress16 - needs compiled and then tested
 * drvEtherLink3 - needs compiled and then tested
 * drvEtherLinkXL - needs compiled and then tested
 * drvIntel82556 - needs compiled and then tested
 * drvIntel82557 - needs compiled and then tested
 * drvIntel82595
 * drvIntel82596
 * drvNE2k - complete
 * drvSMC16 - complete
 * drvSMCUltra - complete
 * drvVMwareVMXNet - complete

scsi
 * drvAdaptec6X60 - needs compiled and then tested
 * drvAdaptec1542B - needs compiled and then tested
 * drvAdaptec2940 - needs compiled and then tested
 * drvAMDPCSCSIDriver - complete
 * drvBusLogic - needs compiled and then tested
 * drvBusLogicFP - needs compiled and then tested
 * drvDPT2000 - needs compiled and then tested
 * drvSym53C8xx - needs compiled and then tested

sound
 * drvBeepSound - needs compiled and then tested
 * drvES1x88Sound - needs compiled and then tested
 * drvIntelAC97Sound - needs compiled and then tested
 * drvSB8Sound - needs compiled and then tested
 * drvSB16Sound - needs compiled and then tested
 * drvSB128Sound - complete

video
 * drv3DfxVoodooVSA - needs compiled and then tested
 * drvATIMach64
 * drvATIRage
 * drvCirrusLogicGD5434
 * drvIBMThinkPad760EDDisplay
 * drvMatroxMGA - complete
 * drvNVidiaRiva - needs compiled and then tested
 * drvS3Generic - complete
 * drvVBoxVideo - complete
 * drvVMWareVideo - complete
 * vga