{
    "VGA" = {
	/* Default display mode configuration for VGA driver */
	"Default Mode" = "640 x 480 x 2 @ 60 Hz";

	/* VGA Standard Modes */
	"Supported Modes" = (
	    "640 x 480 x 2 @ 60 Hz"
	);

	/* Supported Adapters */
	"Supported Chipsets" = (
	    "Standard VGA",
	    "VGA Compatible"
	);
    };
}
