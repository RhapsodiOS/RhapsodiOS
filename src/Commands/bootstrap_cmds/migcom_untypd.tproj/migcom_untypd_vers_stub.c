/* Stage-0 stub: PB vers_string + arch .ppc.o rules do not generate
   migcom_untypd_vers.ppc.o reliably on this tree (NAME vs historical
   migcom_untyped typo). Provide the symbol utils.c expects. */
char migcom_untypd_VERS_STRING[] =
    "@(#)PROGRAM:migcom_untypd  PROJECT:bootstrap_cmds-13.2\n";
