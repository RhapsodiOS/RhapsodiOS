{
    DYNAMIC_CODE_GEN = NO;
    FILESTABLE = {
        CLASSES = (Pro1000.m);
        H_FILES = ();
        OTHER_LINKED = ();
        OTHER_SOURCES = (
            Makefile.preamble,
            Makefile,
            Makefile.postamble,
            Load_Commands.sect
        );
        SUBPROJECTS = ();
    };
    LANGUAGE = English;
    LOCALIZABLE_FILES = {};
    MAKEFILEDIR = "$(MAKEFILEPATH)/pb_makefiles";
    NEXTSTEP_BUILDTOOL = /bin/gnumake;
    PROJECTNAME = Pro1000;
    PROJECTTYPE = "Kernel Server";
    PROJECTVERSION = 1.1;
}
