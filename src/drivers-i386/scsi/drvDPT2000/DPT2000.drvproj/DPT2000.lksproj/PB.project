{
    DRIVERNAME = DPT2000;
    DYNAMIC_CODE_GEN = YES;
    FILESTABLE = {
        CLASSES = (EATAController.m, EATAControllerThread.m);
        H_FILES = (EATAController.h, EATAControllerPrivate.h, EATAControllerTypes.h);
        C_FILES = (EATAControllerRoutines.c);
        OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble, Load_Commands.sect);
    };
    LANGUAGE = English;
    MAKEFILEDIR = "$(MAKEFILEPATH)/pb_makefiles";
    NEXTSTEP_BUILDTOOL = $(GNUMAKE);
    PDO_UNIX_BUILDTOOL = $(GNUMAKE);
    PROJECTNAME = DPT2000;
    PROJECTTYPE = "Kernel Server";
    PROJECTVERSION = 2.7;
    WINDOWS_BUILDTOOL = $(GNUMAKE);
}
