{"entries":[{"address":0,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 0, size 227, 6 basic blocks and 78 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 1; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay _registerWithED], finding 1. [[EventDriver instance] registerScreen:self bounds:&bounds shmem:&priv size:&shmem_size]; token == -1 gives IO_R_INVALID_ARG; shmem_size greater than sizeof(VGAShmem_t) logs '%s: shmem_size > sizeof (VGAShmem_t)(%d<>%d)', unregisters and gives IO_R_INVALID_ARG; otherwise memset(shmem, 0, shmem_size), cursorShow = 1, screenBounds = bounds as two 4-byte stores, [self setToken:token]. One thing the disassembly settles that the finding does not: no cast is needed for the reference's unsigned ja. shmem_size is the int the IOScreenRegistration protocol declares, sizeof is unsigned, and the usual arithmetic conversions make the comparison unsigned by themselves. The 0x1448 literal is the compiled sizeof(VGAShmem_t) from the checked-in IOVGAShared.h and our build emits the same constant from sizeof. 228 bytes against the reference's 228. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay _registerWithED]"],"reason":null,"reviewer":"vga3b-t5-impl","size":227,"source_line":118,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":228,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 228, size 237, 1 basic blocks and 70 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 2; no replacement source has been written, so the status is unexamined.","Reconstructed as SetET4000Brightness(unsigned int level), static, finding 2. Writes the four-entry BW ramp through the DAC write-address and data ports 0x3C8/0x3C9, each entry scaled by level/64 and written three times. The multipliers are IOVGADisplayPrivate.h's WHITE_PALETTE_VALUE 63, LIGHT_GRAY_PALETTE_VALUE 48, DARK_GRAY_PALETTE_VALUE 30 and BLACK_PALETTE_VALUE 0, and the indices its WHITE_INDEX 3 through BLACK_INDEX 0. Two things the disassembly settles that the finding does not: the parameter is unsigned, because the divisions are plain shr (a signed int would need the round-toward-zero fixup) and -setBrightness:token: reaches this after an unsigned cmp 0x40 / jbe; and the scaled value is computed before the index is written, which is why the black entry materialises its zero into cl separately from the zero it writes as the index. Writing the value into a local before each index outb reproduces both. 240 bytes against the reference's 240 (237 of body plus three pad). Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_SetET4000Brightness"],"reason":null,"reviewer":"vga3b-t3-impl","size":237,"source_line":167,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":468,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree on address 468, size 103 and 36 instructions, and disagree only on basic-block count (IDA 8, Ghidra 6): IDA splits the if (colr_mode) diamond one edge finer. angr is disabled for this profile, so this is a two-analyzer consensus; the Mach-O symbol table independently names this address.","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 3; no replacement source has been written, so the status is unexamined.","Reconstructed as select_read_segment(char seg), external, finding 3. vga_reg__in on COLR_CRT or MONO_CRT per colr_mode, index CRT_TS_VS1 (0x36); the two branches differ only in the address port 0x3D4/0x3B4 and both read data from 0x3D5, exactly as the checked-in header defines them, which is why gcc cross-jumps them after the index write. CRT_TS_SGL (0x10) locks the segment select out and returns. The ET4000 Segment Select register at 0x3CD then takes (tmp & GCR_TS_GWR) | (seg << 4) -- the high nibble, with seg shifted and not masked. Written as three statements on tmp rather than one expression, which is what puts tmp in ebx (and gives the push/pop ebx the reference has) instead of a stack slot. 104 bytes against the reference's 104. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"partial"},"artifacts":[],"names":["_select_read_segment"],"reason":null,"reviewer":"vga3b-t3-impl","size":103,"source_line":206,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":572,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree on address 572, size 102 and 36 instructions, and disagree only on basic-block count (IDA 7, Ghidra 6): IDA splits the if (colr_mode) diamond one edge finer. angr is disabled for this profile, so this is a two-analyzer consensus; the Mach-O symbol table independently names this address.","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 4; no replacement source has been written, so the status is unexamined.","Reconstructed as select_write_segment(char seg), external, finding 4. Identical to finding 3 except that the Segment Select write is (tmp & GCR_TS_GRD) | (seg & GCR_TS_GWR) -- the low nibble, with seg masked and not shifted, the mirror image of the read side's asymmetry -- and that it shadows curr_write_segment. 104 bytes against the reference's 104. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"partial"},"artifacts":[],"names":["_select_write_segment"],"reason":null,"reviewer":"vga3b-t3-impl","size":102,"source_line":232,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":676,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 676, size 110, 1 basic blocks and 39 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 5; no replacement source has been written, so the status is unexamined.","Reconstructed as select_read_plane(char plane), external, finding 5. Graphics Controller index GCR_AT_READ_MAPS (4), Read Map Select, taking a plane number: (tmp & ~GCR_AT_RMS) | (plane & GCR_AT_RMS). The checked-in header calls this register block GCR where the finding calls it GDC; COLR_GCR_MSK is 0x0F, giving the and 0xF0 / or 4 pair, and the two macro expansions each re-read and re-write the index register, which is why 0x3CE is touched twice. The masking is written as statements on tmp before the vga_reg_out, which is what puts the or ahead of the second index dance as the reference has it. The Window Server's counterpart sub_703030F0 masks with the same 0xFC and 3 but writes the index register with a bare outb instead of the header's read-modify-write macro, so it clears the index register's high nibble where this side preserves it; the two halves are not harmonised. 112 bytes against the reference's 112. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_select_read_plane"],"reason":null,"reviewer":"vga3b-t3-impl","size":110,"source_line":258,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":788,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 788, size 127, 1 basic blocks and 44 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 6; no replacement source has been written, so the status is unexamined.","Reconstructed as select_write_plane(char plane), external, finding 6. Sequencer index SEQ_AT_MPK (2), Map Mask, taking a plane number and converting it to a one-hot mask: (tmp & 0xF0) | (1 << (plane & 3)). COLR_SEQ_MSK is 0x07, giving the and 0xF8 / or 2 pair. The mask is computed into its own char local before the first read, which the reference shows directly -- an int shift result stored to a stack dword, narrowed to a stack byte, and later or'ed in from memory. The Window Server's counterpart sub_70303144 agrees on the 0xF0 mask and on the one-hot conversion and differs only in the same bare-outb index write as the read side. 128 bytes against the reference's 128. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_select_write_plane"],"reason":null,"reviewer":"vga3b-t3-impl","size":127,"source_line":276,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":916,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 916, size 407, 1 basic blocks and 151 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 7; no replacement source has been written, so the status is unexamined.","Reconstructed as vga_read_bpp4planar_to_bpp2packed32(unsigned short *fb, unsigned int *dst), external, finding 7. Sixteen pixels of planes 1 and 0, each complemented as (unsigned short)~*fb and scattered into alternating bit pairs, plane 0 supplying the low bit of each pair; the two halves are accumulated into separate locals and or'ed at the store, which is the order the reference computes them in. Transcribed unrolled rather than looped. Shift table, plane 1: +2,+5,+8,+11,+14,+17,+20,+23 for source bits 15..8 and -6,-3,0,+3,+6,+9,+12,+15 for bits 7..0; plane 0 is one less throughout. Identical term for term to the Window Server's read_bpp4planar_to_bpp2packed, which returns the word rather than storing it through a pointer but computes it the same way. 408 bytes against the reference's 408, byte for byte. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_vga_read_bpp4planar_to_bpp2packed32"],"reason":null,"reviewer":"vga3b-t3-impl","size":407,"source_line":301,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":1324,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 1324, size 428, 1 basic blocks and 160 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 8; no replacement source has been written, so the status is unexamined.","Reconstructed as vga_write_bpp2packed32_to_bpp4planar(unsigned int *src, unsigned short *fb), external, finding 8. Masks each 16-bit half of the packed word with 0xAAAA (plane 1) or 0x5555 (plane 0), gathers the eight selected bits into a byte, complements it, and stores the two bytes as one 16-bit word; *src is re-read in each pass, which the reference's second load off edi shows. The frame buffer is the second argument here and the first of the reader; that is preserved. The eight gather terms are term for term the Window Server's oddTable and evenTable formulas in build_tables, which is the strongest cross-check available for this body: the two halves compute the same function by different means -- unrolled shifts here, two 64K lookup tables there -- and agree exactly. 428 bytes against the reference's 428, byte for byte. One correction: finding 8's prose claim that 'the shift counts are finding 7's minus one throughout' does not hold. The writer's counts are -15,-12,-9,-6,-3,0,+3,+6 for plane 1 and one greater for plane 0, which is not finding 7's table minus one by any reading; the two functions are inverses, not offsets of one another. The disassembly is what was followed. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_vga_write_bpp2packed32_to_bpp4planar"],"reason":null,"reviewer":"vga3b-t3-impl","size":428,"source_line":341,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":1752,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 1752, size 816, 16 basic blocks and 271 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 9; no replacement source has been written, so the status is unexamined.","Reconstructed as VGADisplayCursor(IODisplayInfo *di, VGAShmem_t *shmem), external, finding 9. Statement for statement the reference: the two read-shadow saves into two locals each, the vga_reg__in of the sequencer map mask, the cursorRect and screenBounds copies, both y clips, the snap of minx to a sixteen-pixel column with c.maxx = c.minx + 32, the saveRect write-back as two dword stores, shift = (cursorRect.minx & 15) * 2 with rshift = 32 - shift, the three cursor pointers at shmem+0x48 + frame*64, +0x148 + frame*64 and +0x248 advanced by the rows the clip took, the two clip flags, the signed idiv 0x10000 / (di->width >> 4) that yields the latent 1638, the unsigned div that splits row into bank and offset, the bank pushed through select_write_segment and select_read_segment on entry and again whenever row % lines is zero, the two-column body with its shift == 0 special case that still steps save, and the restore that feeds the saved *read* values to the *write* selects. Control flow is identical: 23 branches and calls in the same order, the same predicates, the same targets. Not byte-identical -- ours is 772 bytes against the reference's 816. The whole difference is register allocation: our gcc gives the eight-byte Bounds c an esi:edi pair where the reference spills it, which shifts every other local's stack slot by eight and leaves di and row in memory where the reference keeps them in registers, and the reference hoists &vramBuf[1] into a register across the loop where ours pushes the immediate. No statement, constant, operand order or side effect differs; the two clip compares and the leftOK setcc come out with their operands reversed, which is the same predicate read the other way round. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and compared back against it."],"status":"agreed"},"artifacts":[],"names":["_VGADisplayCursor"],"reason":null,"reviewer":"vga3b-t4-impl","size":816,"source_line":410,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"control-flow-confirmed"},{"address":2568,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 2568, size 660, 16 basic blocks and 216 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 10; no replacement source has been written, so the status is unexamined.","Reconstructed as VGARemoveCursor(IODisplayInfo *di, VGAShmem_t *shmem), external, finding 10. 660 bytes against the reference's 660, with all fifteen named locals landing on the reference's exact stack slots (maskL -0x0C, maskR -0x10, scr -0x18, s -0x20, p -0x24, words -0x28, lines -0x2C, shift -0x30, the four saved shadows -0x34 through -0x40 and the map mask at -0x44) and control flow identical -- 23 branches and calls in the same order with the same targets. _mask_array.128 is subscripted with the reference's two expressions, oldCursorRect.minx - saveRect.minx and 16 - (saveRect.maxx - oldCursorRect.maxx), the second complemented; the same signed idiv gives the same 1638; save is read and never written and still steps 32 words per row. One thing the disassembly settled: the reference computes s.miny - scr.miny twice, once for p and the bank and again as the loop initialiser, because the two select calls between them kill the CSE, so the expression is inline in both places rather than held in row. Not byte-identical: our col gets a hard register where the reference spills it, which is the one missing temp slot (frame 0x50 against 0x54), and the reference hoists &vramBuf[1] into a register across the loop where ours pushes the immediate. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and compared back against it."],"status":"agreed"},"artifacts":[],"names":["_VGARemoveCursor"],"reason":null,"reviewer":"vga3b-t4-impl","size":660,"source_line":521,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"control-flow-confirmed"},{"address":3228,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 3228, size 100, 5 basic blocks and 38 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 11; no replacement source has been written, so the status is unexamined.","Reconstructed as -hideCursor:(int)token, finding 11. Byte-for-byte identical to the reference: same 38 instructions, same registers, same order, same 100 bytes; the only textual difference is the absolute addresses of the __OBJC and __text operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1). ev_try_lock is taken on &priv->cursorSema and a failure returns self having done nothing -- this half never blocks and never queues, and ev_lock is not imported by this binary at all. [self displayInfo] is sent before the depth test, not inside it, and token is never read. One thing the disassembly settled: priv is re-read from the ivar for the try-lock and again for the unlock, but the counter and the blitter argument share a single load, so the middle group holds it in a local while the two lock calls do not. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and compared back against it."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay hideCursor:]"],"reason":null,"reviewer":"vga3b-t4-impl","size":100,"source_line":629,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":3328,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 3328, size 517, 25 basic blocks and 167 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 12; no replacement source has been written, so the status is unexamined.","Reconstructed as -moveCursor:frame:token:, finding 12. The Window Server's VGASysHideCursor, VGACheckShield and VGASysShowCursor inlined into one method, which is why the show tail appears twice and why both copies run in sequence when the shield clears -- VGACheckShield calls VGASysShowCursor and the caller calls it again. frame and cursorLoc are written first, then the hide, then the un-obscure that decrements the depth without asking the Window Server, then the shield test, whose rectangle is recomputed into locals and deliberately not written to cursorRect, with four strict compares so an edge-touching intersection is a miss. Control flow is identical: 26 branches and calls in the same order with the same targets, and the shield block's locals land on the reference's slots. 528 bytes against the reference's 520; the eight bytes are two redundant reloads our compiler emits for cursorRect.minx and .miny after storing them, plus a register copy before each add of CURSORWIDTH. Two things the disassembly settled: shielded is stored before the hide/show branch and the branch then tests the byte just stored, and the hot spot is read as one four-byte Point and split with sar 16. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and compared back against it."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay moveCursor:frame:token:]"],"reason":null,"reviewer":"vga3b-t4-impl","size":517,"source_line":662,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"control-flow-confirmed"},{"address":3848,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 3848, size 453, 20 basic blocks and 147 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 13; no replacement source has been written, so the status is unexamined.","Reconstructed as -showCursor:frame:token:, finding 13. Finding 12 without the leading hide and without the un-obscure step, and with the final [self displayInfo] sent before the depth test rather than inside it -- a real difference between the two methods, read out of the two disassemblies. Control flow is identical: 21 branches and calls in the same order with the same targets. 468 bytes against the reference's 456, the twelve bytes being the same two redundant reloads as finding 12 plus one register copy in each of the two show tails. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and compared back against it."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay showCursor:frame:token:]"],"reason":null,"reviewer":"vga3b-t4-impl","size":453,"source_line":743,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"control-flow-confirmed"},{"address":4304,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4304, size 47, 1 basic blocks and 15 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 14; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay generateNameAndUnit:], finding 14. *unit = nextVGAUnit++; sprintf(nameBuf, 'VGADisplay%d', *unit); return nameBuf. The sprintf argument is read back through the caller's pointer rather than reusing the value just stored, which is the reference's second 'mov eax,[eax]'. The return type is plain char *, per Task 2's __meth_var_types correction to the finding. 48 bytes against the reference's 48. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay generateNameAndUnit:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":47,"source_line":816,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4352,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4352, size 9, 1 basic blocks and 6 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 15; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay map], finding 15: return 0. A stub in the reference too -- the frame buffer is not mapped through this path -- which exists so the IO_Framebuffer_Map parameter has something to call. 12 bytes against the reference's 12. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay map]"],"reason":null,"reviewer":"vga3b-t5-impl","size":9,"source_line":828,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4364,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4364, size 84, 4 basic blocks and 39 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 16; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay unmap], finding 16: n = [[self deviceDescription] numPortRanges]; for (i = 0; i < n; i++) [self releasePortRange:i]. Both i and n are unsigned, giving the reference's jae/jb pair, so a device with no port ranges releases nothing. 84 bytes against the reference's 84. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay unmap]"],"reason":null,"reviewer":"vga3b-t5-impl","size":84,"source_line":837,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4448,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4448, size 144, 1 basic blocks and 53 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 17; no replacement source has been written, so the status is unexamined.","Reconstructed as +[IOVGADisplay probe:], finding 17, with Apple's defect reproduced rather than fixed: the method returns YES unconditionally and never tests initFromDeviceDescription:'s result, so generateNameAndUnit:, setUnit:, setName:, setDeviceKind:'frame buffer' and registerDevice are all sent to a receiver that may be nil. 144 bytes against the reference's 144. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["+[IOVGADisplay probe:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":144,"source_line":852,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4592,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4592, size 41, 1 basic blocks and 15 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 18; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay free], finding 18: return [super free]. Neither the shared region nor the vidBIOS instance is released. 44 bytes against the reference's 44. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay free]"],"reason":null,"reviewer":"vga3b-t5-impl","size":41,"source_line":871,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4636,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4636, size 358, 17 basic blocks and 110 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 19; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay initFromDeviceDescription:], finding 19. Two things the disassembly settles against the finding's prose, and the disassembly wins in both. First, the 'Yes' test is strcmp, not strncmp: gcc rewrites strcmp against a literal into an inline four-byte repe cmpsb, which is the reference's 'mov ecx,4' at 4744, whereas the strncmp in -didBootWithDefaultConfig is a real call. Second, and substantively, -didBootWithDefaultConfig is NOT dead code. The store that sets svga_bios_mode to 1 at 4756 is followed by 'jmp 4775' (eb 0a), which lands past the else branch's store at 4768 and on the didBootWithDefaultConfig call itself, so the call runs on both paths and its YES answer clears the flag the 'Yes' branch just set. Booting with config=Default really does suppress SVGA mode. svga_bios_mode is a char, not an int: all eight accesses in the reference are byte-sized. 360 bytes against the reference's 360. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay initFromDeviceDescription:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":358,"source_line":892,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":4996,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 4996, size 64, 3 basic blocks and 27 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 20; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay setBrightness:token:], finding 20, with Apple's defect reproduced rather than fixed: the range check logs and does not guard, so both paths fall into SetET4000Brightness. The comparison is unsigned -- (unsigned int)level > 64 gives the reference's 'cmp ebx,0x40 / jbe' -- which independently confirms Task 3's finding that SetET4000Brightness takes an unsigned int. token is unread. 64 bytes against the reference's 64. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay setBrightness:token:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":64,"source_line":937,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":5060,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 5060, size 437, 20 basic blocks and 139 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 21; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay getIntValues:forParameter:count:], finding 21. Four strcmp compares in the reference's order -- IO_Framebuffer_Map, IO_Framebuffer_Dimensions, IOGetDisplayInfo, IO_Framebuffer_Register -- then objc_msgSendSuper. gcc turns each strcmp against a literal into an inline repe cmpsb of strlen+1 bytes, which is where the reference's 19, 26, 17 and 24 come from; no strncmp is written anywhere. The IOGetDisplayInfo triples are the hardcoded immediates {800,600,200} and {640,480,160}, chosen on svga_bios_mode. This method is where registerDisplay: is sent, which no finding accounted for: the IO_Framebuffer_Register path sends [self _registerWithED], keeps its IOReturn as the method's result, then sends [kmId registerDisplay:self] to the kernel's console device. 428 bytes against the reference's 440. Read against the full reference disassembly instruction by instruction and diffed back against our own build's output: every statement, constant, string, call, branch and side effect matches. The residual 12 bytes is register allocation only -- the reference spills the count pointer to its stack home at entry and reloads it for the IOGetDisplayInfo check (4 frame bytes plus 6 instruction bytes) where ours keeps it in ebx, and the reference emits a dead 'mov esi,eax' after the msgSendSuper tail. The rest is ecx/edx scratch choice. Not claimed as assembly-matched."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay getIntValues:forParameter:count:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":437,"source_line":962,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"control-flow-confirmed"},{"address":5500,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 5500, size 295, 14 basic blocks and 104 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 22; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay setIntValues:forParameter:count:], finding 22. Four strcmp compares -- IO_Framebuffer_Unmap, IO_Framebuffer_SetDimensions, IO_Framebuffer_Unregister, Set VGA VESA Mode -- then objc_msgSendSuper. SetDimensions never inspects count and writes bitsPerPixel = IO_VGA at IODisplayInfo offset 0x18; Set VGA VESA Mode requires count == 1 and then sends [self enterSVGAMode:vesaMode] using the driver's own global, discarding parameterArray[0]. The reference cross-jumps the Unregister path's final objc_msgSend into the enterSVGAMode: path's, and our build reproduces that instruction for instruction. 296 bytes against the reference's 296. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay setIntValues:forParameter:count:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":295,"source_line":1038,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":5796,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 5796, size 29, 1 basic blocks and 12 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 23; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay allocateConsoleInfo], finding 23: a tail call to VGAAllocateConsole([self displayInfo]), the undefined-and-kernel-resolved entry in src/kernel-7/bsd/dev/i386/VGAConsole.c. IOConsoleInfo comes from bsd/dev/i386/ConsoleSupport.h, which driverkit/IODisplayPrivate.h already pulls in. 32 bytes against the reference's 32. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay allocateConsoleInfo]"],"reason":null,"reviewer":"vga3b-t5-impl","size":29,"source_line":1083,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":5828,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 5828, size 114, 3 basic blocks and 42 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 24; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay(VESAMode) enterSVGAMode:], finding 24. memset(&regs, 0, 64); regs.eax = 0x4F02 (VESA set SuperVGA video mode); regs.ebx = mode; [self int10:&regs]; then (unsigned short)regs.eax != 0x004F logs '%s: BIOS mode change returned %04x' and sleeps five seconds so the operator can read it before the console is repainted. The store of mode at frame offset 0x0C independently confirms that ebx is index 3 of the register block. 116 bytes against the reference's 116. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay enterSVGAMode:]"],"reason":null,"reviewer":"vga3b-t5-impl","size":114,"source_line":1097,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":5944,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 5944, size 166, 1 basic blocks and 59 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 25; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay(VESAMode) int10:], finding 25: the alloca'd IORange vector of numPortRanges + 1 entries with the wide-open static const ports = {0, 0x10000} first, the device's own ranges memcpy'd after it, and the same register block passed as both inregs and outregs -- which is why -enterSVGAMode: can read its result out of the block it filled in. 156 bytes against the reference's 168, and finding 25 already ruled this intentional-mismatch: the reference copies NINE bytes out of _ports.168 (two mov dwords and one mov byte, the ninth byte read landing on _VGA_VERS_STRING[0] and the ninth byte written on ranges[1].start, which the memcpy immediately overwrites) where IORange in driverTypes.h is eight, so 'ranges[0] = ports;' correctly emits two moves rather than three. That accounts for 10 of the 12 bytes; the other 2 are a redundant 'mov ecx,eax' the reference emits after portRangeList. Read against the full reference disassembly instruction by instruction: no statement, constant, call, branch or side effect differs, and the remainder is ecx/edx scratch allocation."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay int10:]"],"reason":"Reconstructed as -[IOVGADisplay(VESAMode) int10:], finding 25: the alloca'd IORange vector of numPortRanges + 1 entries with the wide-open static const ports = {0, 0x10000} first, the device's own ranges memcpy'd after it, and the same register block passed as both inregs and outregs -- which is why -enterSVGAMode: can read its result out of the block it filled in. 156 bytes against the reference's 168, and finding 25 already ruled this intentional-mismatch: the reference copies NINE bytes out of _ports.168 (two mov dwords and one mov byte, the ninth byte read landing on _VGA_VERS_STRING[0] and the ninth byte written on ranges[1].start, which the memcpy immediately overwrites) where IORange in driverTypes.h is eight, so 'ranges[0] = ports;' correctly emits two moves rather than three. That accounts for 10 of the 12 bytes; the other 2 are a redundant 'mov ecx,eax' the reference emits after portRangeList. Read against the full reference disassembly instruction by instruction: no statement, constant, call, branch or side effect differs, and the remainder is ecx/edx scratch allocation.","reviewer":"vga3b-t5-impl","size":166,"source_line":1124,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"intentional-mismatch"},{"address":6112,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6112, size 106, 13 basic blocks and 51 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 26; no replacement source has been written, so the status is unexamined.","Reconstructed as find_parameter(const char *key, char *s), static, finding 26. strlen(key) is the inline repne scasb form -- there is no _strlen in the reference's symbol table -- while strncmp is a real call taking (s, key, len) in that order. One thing the disassembly settles that the finding's C does not: the blank-skipping loop caches *s in an int (movsx from the char, tested against zero before ' ' and tab), and that same cached value feeds the final 'return *s ? s : 0', which gcc emits as one cross-jumped tail shared with the return-zero path. Writing it as while ((c = *s) != 0 && (c == ' ' || c == tab)) s++; return c ? s : 0; reproduces that exactly, where the finding's re-dereferencing form compiles eight bytes short and re-reads the byte three times. 108 bytes against the reference's 108. Read against the full reference disassembly instruction by instruction, disassembled out of the Mach-O with capstone rather than taken from the finding's pseudocode, and our own build's body was disassembled and diffed back against it: the two instruction streams are identical, in the same order with the same registers and the same body length, the only textual difference being the absolute addresses of the __DATA operands, which differ because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1)."],"status":"agreed"},"artifacts":[],"names":["_find_parameter"],"reason":null,"reviewer":"vga3b-t3-impl","size":106,"source_line":1157,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":6220,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6220, size 149, 20 basic blocks and 59 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 27; no replacement source has been written, so the status is unexamined.","Reconstructed as -[IOVGADisplay(VESAMode) didBootWithDefaultConfig], finding 27. KERNSTRUCT_ADDR's magicCookie against KERNBOOTMAGIC, then find_parameter('config', kbs->bootString), blank skipping, '=', blank skipping, strncmp(p, 'Default', 7) as a real call, and a terminator test. The two literal addresses gcc folds -- 0x110A4 and 0x11002 -- confirm machdep/i386/kernBootStruct.h exactly, so that header needs no change. The tail is written as 'if (...) return YES; return NO;' and not as a ternary: the ternary materialises the result through edx and costs four bytes the reference does not spend. Contrary to finding 19's prose this method's answer is used; see the entry for 4636. 152 bytes against the reference's 152. Read against the full reference disassembly instruction by instruction (disassembled out of the Mach-O with capstone, not taken from the finding's prose) and our own build's body was disassembled and diffed back against it: the two instruction streams are identical -- same mnemonics, same operands, same registers, same order, same body length. The only textual difference is the absolute addresses of the __OBJC, __DATA and __cstring operands, which sit lower in our image because our __TEXT,__const is 170 bytes shorter (the build-generated _VGA_VERS_STRING / _VGA_VERS_NUM, already intentional-mismatch from Task 1), and because our link places emu486.o first so every IOVGADisplay.m body sits four bytes higher."],"status":"agreed"},"artifacts":[],"names":["-[IOVGADisplay didBootWithDefaultConfig]"],"reason":null,"reviewer":"vga3b-t5-impl","size":149,"source_line":1187,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/IOVGADisplay.m","status":"assembly-matched"},{"address":6372,"analyzer_agreement":{"analyzers":["IDA"],"reasons":["Ghidra does not recover a function at 6372 at all. IDA does, and the Mach-O symbol table names this address with the next symbol at 6384, so the 12-byte extent is not in doubt; but with angr disabled for this profile the entry rests on IDA plus the linker-emitted symbol table rather than on two analyzers. Recorded as reduced coverage (divergences.md, VGA_reloc finding 28).","No source counterpart exists and none will be written: this entry is emitted by the Kernel Server project type, not by hand (divergences.md, VGA_reloc finding 28). The source map places it in unmapped under the build-generated reason class."],"status":"partial"},"artifacts":[],"names":["+[VGAKernelServerInstance kernelServerInstance]"],"reason":"The Kernel Server project type generates this method from the .lksproj's NAME; it returns the address of the __common symbol _VGA_instance that the same project type names in the Loaded Server 'Instance Var' section. It is not hand-written source and cannot be reproduced by writing any, so it is recorded as an intentional mismatch rather than left for the rewrite (divergences.md, VGA_reloc finding 28).","reviewer":"Pat Raynor","size":12,"source_line":null,"source_path":null,"status":"intentional-mismatch"},{"address":6384,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6384, size 12, 1 basic blocks and 6 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists and none will be written: this entry is emitted by the Kernel Server project type, not by hand (divergences.md, VGA_reloc finding 29). The source map places it in unmapped under the build-generated reason class."],"status":"agreed"},"artifacts":[],"names":["+[VGAVersion driverKitVersionForVGA]"],"reason":"The Kernel Server project type generates this method from the project's DriverKitVersion; it returns the constant 500. It is not hand-written source and cannot be reproduced by writing any, so it is recorded as an intentional mismatch rather than left for the rewrite (divergences.md, VGA_reloc finding 29).","reviewer":"Pat Raynor","size":12,"source_line":null,"source_path":null,"status":"intentional-mismatch"},{"address":6396,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6396, size 267, 11 basic blocks and 82 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 30; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS init]"],"reason":null,"reviewer":"vga3b-t6-impl","size":267,"source_line":40,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"assembly-matched"},{"address":6664,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6664, size 107, 5 basic blocks and 34 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 31; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS free]"],"reason":null,"reviewer":"vga3b-t6-impl","size":107,"source_line":69,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"assembly-matched"},{"address":6772,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 6772, size 696, 21 basic blocks and 232 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 32; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS int10:outregs:iorange:ionum:smmport:]"],"reason":null,"reviewer":"vga3b-t6-impl","size":696,"source_line":89,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"control-flow-confirmed"},{"address":7468,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 7468, size 44, 1 basic blocks and 19 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 33; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS int10:outregs:iorange:ionum:]"],"reason":null,"reviewer":"vga3b-t6-impl","size":44,"source_line":166,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"assembly-matched"},{"address":7512,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 7512, size 16, 1 basic blocks and 8 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 34; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS scratchSegment]"],"reason":null,"reviewer":"vga3b-t6-impl","size":16,"source_line":178,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"assembly-matched"},{"address":7528,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 7528, size 22, 1 basic blocks and 10 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 35; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["-[vidBIOS realToVirtual::]"],"reason":null,"reviewer":"vga3b-t6-impl","size":22,"source_line":184,"source_path":"src/drivers-i386/video/drvVGA/VGA.drvproj/VGA.lksproj/vidBIOS.m","status":"assembly-matched"},{"address":7552,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra disagree on this function's extent: IDA gives the primary chunk 8088 bytes and 1431 instructions, Ghidra 6931 and 1408, and they fragment the remainder along entirely different boundaries. angr numerically agreed with IDA on 7552/8088 but produced a phantom six-byte function at 7760 that misdisassembled bytes both other tools read correctly, which is why it is disabled for this profile. The size recorded here is IDA's, which is what the source map uses. No analyzer's interior boundaries are trusted; the 67 unnamed fragments inside the extent (1161 bytes, 7912-15206) are the per-opcode handlers and the 2288 bytes from 15760 to the end of __text are dispatch tables (divergences.md, VGA_reloc finding 36).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 36; no replacement source has been written, so the status is unexamined."],"status":"partial"},"artifacts":[],"names":["_emu486"],"reason":null,"reviewer":null,"size":8088,"source_line":null,"source_path":null,"status":"unexamined"},{"address":15640,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA gives this 81 bytes and 37 instructions, reaching the last ret of the sixteen setcc stubs; Ghidra gives 17 bytes and 5, taking only the dispatch head and treating each stub as a separate body. The source map and this ledger use IDA's 81, which is the extent the jump table jpt_3D22 at 17984 covers. angr is disabled for this profile (divergences.md, VGA_reloc finding 37).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 37; no replacement source has been written, so the status is unexamined."],"status":"partial"},"artifacts":[],"names":["sub_00003D18"],"reason":null,"reviewer":null,"size":81,"source_line":null,"source_path":null,"status":"unexamined"},{"address":15721,"analyzer_agreement":{"analyzers":["Ghidra","IDA"],"reasons":["IDA and Ghidra agree exactly on address 15721, size 37, 3 basic blocks and 12 instructions. angr is disabled for this profile because its CFGFast misdisassembles six bytes at 7760 inside _emu486 and breaks consensus normalization, so this is a two-analyzer consensus (divergences.md, Evidence).","No source counterpart exists. Our VGA.lksproj implements a class VGA subclassing IOFrameBufferDisplay and shares no symbol, string or class name with Apple's IOVGADisplay, IOVGADisplay(VESAMode), vidBIOS and _emu486 inventory, so the source map places this entry in unmapped under the 'no counterpart in our source' reason class. The reference behaviour is decompiled in divergences.md, VGA_reloc finding 38; no replacement source has been written, so the status is unexamined."],"status":"agreed"},"artifacts":[],"names":["sub_00003D69"],"reason":null,"reviewer":null,"size":37,"source_line":null,"source_path":null,"status":"unexamined"}],"rebuilt_sha256":null,"reference_sha256":"489D86652B8718237052F10A551FC6615B4B045F6614DCB790CFB73272C15B96","schema_version":"ledger-v1"}
