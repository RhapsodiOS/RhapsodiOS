/*
 * DEC21x4KDB.m - STUB, no in-tree source.
 *
 * This file was scaffolded, not copied: no source for the DEC21x4Ethernet
 * bundle exists anywhere in this tree. The selector below was extracted
 * from the shipped DEC21x4Ethernet.config binary's Objective-C metadata
 * (DEC21x4Ethernet_reloc, read with binrecon.macho.read_macho), which
 * named this category DEC21x4KDB (kernel debugger). The method body here
 * is empty: it returns a zero value of its declared return type and does
 * nothing else. Nothing in this file implements Ethernet driver
 * behaviour -- see DEC21x4.h for the superclass determination.
 */

#import "DEC21x4.h"

@implementation DEC21x4(DEC21x4KDB)

- (void)sendPacket:(void *)pkt length:(unsigned int)pktLength
{
}

@end
