{
    FILESTABLE = {
        C_FILES = ();
        HEADERSEARCH = (../PortServer.lksproj);
        H_FILES = (IODeviceMaster.h, pdservd.h);
        M_FILES = (pdservd.m, IODeviceMaster.m);
        OTHER_LINKED = ();
        OTHER_SOURCES = (Makefile, Makefile.preamble, driverServer.defs);
    };
    GENERATEMAIN = YES;
    LANGUAGE = English;
    LOCALIZABLE_FILES = {};
    NEXTSTEP_BUILDTOOL = /bin/gnumake;
    NEXTSTEP_COMPILEROPTIONS = "-DDRIVER_PRIVATE";
    NEXTSTEP_INSTALLDIR = "$(NEXT_ROOT)/private/Drivers";
    PROJECTNAME = pdservd;
    PROJECTTYPE = Tool;
    PROJECTVERSION = 1.0;
}
